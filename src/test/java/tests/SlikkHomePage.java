package tests;

import base.BaseTest;
import org.openqa.selenium.WebDriver;
import org.testng.annotations.Test;
import pages.SlikkHomePa;

public class SlikkHomePage extends BaseTest {


     /*   @Test
        public  void LaunchSlikk() throws InterruptedException {
             SlikkHomePa slikkhomePage = new SlikkHomePa(driver);

             assert(slikkhomePage.getTitle().equals("Slikk | Get your fit delivered in under 60mins"));

             if(slikkhomePage.cardLinkText().isDisplayed()){
                 slikkhomePage.cardLinkText().click();
             }

             Thread.sleep(3000);

//             slikkhomePage.mensSections().click();
//             Thread.sleep(3000);
//
//             //slikkhomePage.Carousel();
        }
*/
}
